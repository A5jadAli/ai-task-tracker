# AI Provider Configuration
# Choose 'gemini' or 'openai'
MODEL_PROVIDER=gemini
MODEL_NAME=gemini-2.0-flash-exp

# API Keys (Replace with your actual keys)
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# ============================================================================
# SLACK INTEGRATION - Personal Account Mode
# ============================================================================
# For the agent to act as YOUR personal Slack account (not a bot)
#
# How to get SLACK_USER_TOKEN:
# 1. Go to https://api.slack.com/apps
# 2. Create a new app or select existing app
# 3. Go to "OAuth & Permissions"
# 4. Add these User Token Scopes:
#    - users:read (View people in workspace)
#    - channels:read (View basic channel info)
#    - channels:history (View messages in public channels)
#    - chat:write (Send messages as you)
#    - groups:read (View basic private channel info)
#    - groups:history (View messages in private channels)
#    - im:read (View basic DM info)
#    - im:history (View messages in DMs)
#    - mpim:read (View basic group DM info)
#    - mpim:history (View messages in group DMs)
# 5. Install app to your workspace
# 6. Copy the "User OAuth Token" (starts with xoxp-)
#
# How to get SLACK_USER_ID:
# 1. Open Slack desktop/web app
# 2. Click your profile picture
# 3. Click "Profile"
# 4. Click "More" (three dots)
# 5. Click "Copy member ID"
#
SLACK_USER_TOKEN=xoxp-your-user-token-here
SLACK_USER_ID=U01234567890

# Legacy Bot Mode (Optional - keep for backward compatibility)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# ============================================================================
# GITHUB INTEGRATION - Personal Account Mode
# ============================================================================
# For the agent to act as YOUR personal GitHub account
#
# How to get GITHUB_PERSONAL_TOKEN:
# 1. Go to https://github.com/settings/tokens/new
# 2. Give it a descriptive name (e.g., "AI Task Tracker Agent")
# 3. Set expiration (recommend 90 days for security)
# 4. Select these scopes:
#    - repo (Full control of private repositories)
#    - notifications (Access notifications)
#    - read:user (Read user profile data)
#    - read:org (Read org and team membership)
# 5. Click "Generate token"
# 6. Copy the token (starts with ghp_)
#
# How to get GITHUB_USERNAME:
# - This is your GitHub username (e.g., if your profile is github.com/johndoe, use "johndoe")
#
GITHUB_PERSONAL_TOKEN=ghp_your_personal_access_token_here
GITHUB_USERNAME=your-github-username

# Legacy Token (Optional - keep for backward compatibility)
GITHUB_TOKEN=ghp_your_github_token

# ============================================================================
# AGENT CONFIGURATION
# ============================================================================
HUMAN_IN_THE_LOOP=false

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/agent.log

# Rate Limiting
GEMINI_RATE_LIMIT_PER_MINUTE=15
OPENAI_RATE_LIMIT_PER_MINUTE=60

# Cache Settings
CACHE_TTL_MINUTES=60
ENABLE_CACHE=true
